<aside class="newsletter | flow">
  <h3>{{ meta.newsletter.title }}</h3>
  <form data-hubspot class="flow">
    <label class="form-control gutter-xs text-step-min-1">
      <input type="checkbox" name="consent" required />
      {{ meta.newsletter.consent_message | markdownFormat | safe }}
    </label>

    <div class="sidebar" data-direction="rtl">
      <div class="flow ">
        <label class="visually-hidden"> Subscribe to Newsletter</label>
        <input type="email" name="email" autocomplete="email" placeholder="Type your email..." required />
      </div>

      <button class="button" type="submit" data-button-variant="gold">Submit</button>
    </div>
  </form>

  <div data-hubspot-message>
    <div data-hubspot-success class="hidden">{{ meta.newsletter.success_message }}</div>
    <div data-hubspot-error class="hidden">{{ meta.newsletter.error_message }}</div>
    <div data-hubspot-consent-error class="hidden">{{ meta.newsletter.consent_required }}</div>
  </div>
</aside>

{% css "inline" %}
  {% include "css/form.css" %}
{% endcss %}

{% js "defer" %}
  {% include "scripts/hubspot-submission.js" %}
{% endjs %}
