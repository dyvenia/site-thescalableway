---
title: About
permalink: /about/index.html
description: 'At The Scalable Way, we believe that data-driven success comes from precise execution and scalable solutions.'
layout: base
bodyClass: about
footerSpace: mt-2xl
intro: |
  At The Scalable Way, we believe that data-driven success comes from precise execution and scalable solutions.

  Our mission is to empower businesses by building resilient data platforms, streamlining analytics pipelines, and optimizing data science workflows — all with an "as a code" approach that ensures reliability, agility, and long-term value.

  With a team of experts passionate about data infrastructure and cloud-native technologies, we transform complex data challenges into scalable, actionable solutions tailored to our clients' unique needs.
---

<div class="wrapper">
  <header class="region flow">
    <h1>{{ title }}</h1>
    <div class="flow">{{ intro | markdownFormat | safe }}</div>
  </header>
  <article class="feature flow flow-space-l">
    {% for member in collections.team_members %}
      {% if member.data.show_on_about %}
        <div class="about__content flow grid gutter-m" data-layout="50-50">
          {% image "." + member.data.image, "" %}
          <div class="flow prose">
            <h3>{{ member.data.name }}</h3>
            {{ member.data.description_long | markdownFormat | safe }}
          </div>
        </div>
      {% endif %}
    {% endfor %}
  </article>
</div>

{%- css "local" -%}
  {%- include 'css/about.css' -%}
{%- endcss -%}
